version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MONGODB_URI=mongodb+srv://riteshoptimatrix_db_user:ZJIKusaw67Ihx1BL@reviewdbcluster.a9tkxni.mongodb.net/?appName=reviewdbcluster
      - MONGODB_DB_NAME=business_cache
      - DEBUG_EXTRACTION=false
      - HTTP_TIMEOUT_SECONDS=20
      - MAX_REDIRECTS=10
      - API_KEY=dev_key
      - RATE_LIMIT_REQUESTS=100
      - RATE_LIMIT_WINDOW_SECONDS=60
      - MAX_CONCURRENT_REQUESTS=10
